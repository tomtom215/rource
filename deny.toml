# cargo-deny configuration
# https://embarkstudios.github.io/cargo-deny/

[graph]
# Targets to check
targets = []
all-features = true

[advisories]
# The URLs of the advisory databases
db-urls = ["https://github.com/rustsec/advisory-db"]
# Ignore specific advisories (add IDs here if you need to allow specific issues)
ignore = [
    # paste is unmaintained but not a security issue - transitive dep from wgpu via metal
    # https://rustsec.org/advisories/RUSTSEC-2024-0436
    "RUSTSEC-2024-0436",
]

[licenses]
# Allow common permissive licenses and GPL-3.0-only (our license)
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Zlib",
    "Unlicense",
    "Unicode-3.0",
    "GPL-3.0-only",
]
# Confidence threshold for license detection
confidence-threshold = 0.8

[bans]
# Lint level for multiple versions of the same crate
multiple-versions = "warn"
# Wildcards in dependencies - allow for workspace path dependencies
wildcards = "allow"
# Allow specific crates with multiple versions (if needed)
skip = []

[sources]
# Allow crates from crates.io
unknown-registry = "deny"
# Allow crates from git (for development)
unknown-git = "warn"
# List of allowed registries
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
